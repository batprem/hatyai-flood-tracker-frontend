[serve.static]
plugins = ["bun-plugin-tailwind"]
# Inline `process.env.<NAME>` literals at dev/build time. `VITE_*` follows the
# project-wide convention (e.g. `VITE_API_URL` for the backend base URL).
env = "VITE_*"
